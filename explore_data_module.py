def explore_data(name, dataset):
    length_row = len(dataset)
    length_col = len(dataset[0])

    print('The total number of rows for', name, 'is: ', length_row)
    print('The total number of columns for', name, 'is: ', length_col)